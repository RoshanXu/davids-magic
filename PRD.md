# DavidMagic（大卫的魔法工具）— 产品需求文档

## 1. 产品概述

| 项目 | 说明 |
|------|------|
| **产品名称** | 大卫的魔法工具 / DavidMagic |
| **产品定位** | 面向老年用户的本地音视频处理工具 |
| **核心功能** | 基于 ffmpeg 实现音视频合并、剪辑、格式转换 |
| **目标平台** | Windows、Android（Flutter 版）；Web（网页版，当前线上重点，主要微信内使用） |
| **语言** | 简体中文 |
| **设计原则** | 界面简洁、字体大号、操作流程清晰、防误操作 |

---

## 2. 技术方案

| 项目 | 选型 |
|------|------|
| **跨平台框架** | Flutter |
| **ffmpeg 方案** | `ffmpeg_kit_flutter` 内置打包，用户无需单独安装 |
| **安装包体积** | 预计 50-80MB（含 ffmpeg 库） |

> 📌 **两套实现（2026-08 更新）**：本产品目前有**两套实现**——Flutter 原生版（`lib/`，Windows/Android，早期开发、功能较全）和 **Web 版**（`web/index.html` + FFmpeg.wasm，本地打包无 CDN，Docker/nginx 部署，**当前线上部署重点**，主要在微信里用）。本 PRD 描述产品目标；具体技术实现、目录结构与加载链路见 `CLAUDE.md`。

---

## 3. 功能需求

### 3.1 文件管理

| 功能 | 描述 |
|------|------|
| **添加文件** | 点击「+ 添加文件」按钮，弹出系统文件选择器，自动过滤仅显示音视频格式 |
| **文件列表** | 已添加文件以列表展示，每项显示：文件名、时长、分辨率（视频）/ 码率（音频） |
| **拖拽排序** | 拖拽调整文件顺序（用于合并拼接） |
| **移除文件** | 每个文件右侧「✕」按钮删除单个 |
| **清空全部** | 列表底部「清空全部」按钮 |
| **多文件用途** | 多文件仅作为拼接素材，最终导出为一个文件 |

### 3.2 合并

| 功能 | 描述 |
|------|------|
| **合并类型** | 音频 + 音频 → 一个音频；视频 + 视频 → 一个视频 |
| **合并方式** | 按文件列表顺序首尾相接拼接 |
| **交互** | 拖拽调整顺序 → 预览确认 → 输出 |
| **约束** | 不同格式/参数的文件可混合拼接，按最高参数统一输出 |

### 3.3 剪辑

| 功能 | 描述 |
|------|------|
| **时间裁剪** | 选取视频/音频的某段时间段保留，其余丢弃 |
| **片段删除** | 删除中间某段时间段，保留头尾 |
| **时间格式** | `时:分:秒.毫秒`，如 `02:15:30.500`，精确到毫秒级 |
| **交互方式** | 大号时间输入框，配合进度条式时间轴辅助选取 |

### 3.4 预览

| 功能 | 描述 |
|------|------|
| **预览触发** | 剪辑设置后，点击「▶ 预览」按钮 |
| **预览内容** | 播放选中区间的片段（前后各 3 秒延伸） |
| **预览形式** | 弹窗播放，非强制 |

### 3.5 输出

| 功能 | 描述 |
|------|------|
| **功能合并** | 格式转换 + 导出统一为一个「输出」按钮 |
| **有编辑时** | 输出处理后的成品文件 |
| **无编辑时** | 等同于格式转换，选格式即可导出 |

#### 导出设置面板

| 设置项 | 说明 |
|------|------|
| **输出格式** | 视频：MP4 / MKV / MOV / AVI / WebM / GIF；音频：MP3 / AAC / WAV / FLAC / OGG（默认：视频 MP4，音频 MP3） |
| **画质预设** | 以输入文件最高参数为上限：视频：原画 / 高清(1080p) / 标清(720p) / 小文件(480p)；音频：无损 / 高质量(320kbps) / 标准(192kbps) / 小文件(128kbps)。预设名称用通俗描述，不显示技术参数 |
| **输出路径** | 选择保存文件夹 |
| **输出文件名** | 可修改默认文件名 |
| **预估大小** | 显示预估输出文件大小 |

### 3.6 进度与后台

| 功能 | 描述 |
|------|------|
| **进度弹窗** | 进度条（百分比）+ 预估剩余时间 + 取消按钮 |
| **后台处理** | 可最小化进度窗口，处理完成后通知用户 |
| **取消处理** | 支持中途取消，已生成的部分文件自动清理 |

### 3.7 自动保存草稿

| 功能 | 描述 |
|------|------|
| **触发时机** | 文件列表、剪辑区间、排序发生变化时自动保存 |
| **存储内容** | 文件路径列表 + 各自剪辑区间 + 排列顺序 + 上次导出设置 |
| **恢复机制** | 下次启动时检测到未完成草稿，弹窗询问「是否恢复上次未完成的任务？」 |
| **存储策略** | 仅保留一份草稿，新任务覆盖旧草稿 |

---

## 4. 界面设计

### 4.1 布局结构

```
┌─────────────────────────────────┐
│         DavidMagic              │  ← 标题栏
│                          ☀/☾    │  ← 主题切换
├─────────────────────────────────┤
│  📁 文件列表                    │
│  ┌─────────────────────────┐   │
│  │ 1. video_01.mp4    ✕   │   │  ← 可拖拽排序
│  │    时长: 05:30  1080p  │   │
│  │    剪辑: 01:00~04:30   │   │  ← 显示已设剪辑区间
│  ├─────────────────────────┤   │
│  │ 2. audio_01.mp3    ✕   │   │
│  │    时长: 03:20  320kbps│   │
│  ├─────────────────────────┤   │
│  │ + 添加文件              │   │
│  └─────────────────────────┘   │
│         [清空全部]              │
├─────────────────────────────────┤
│                                 │
│   [🔧 剪辑]  [🔗 合并]  [📤 输出] │  ← 大号操作按钮
│                                 │
├─────────────────────────────────┤
│  状态栏 / 处理进度              │
└─────────────────────────────────┘
```

### 4.2 字体规范

| 元素 | 字号 | 字重 |
|------|------|------|
| 标题 | 22px | Bold |
| 功能按钮 | 18-20px | Bold |
| 文件列表项 | 16-18px | Regular |
| 标签/说明文字 | 14-16px | Regular |
| 最小字号 | ≥14px | — |

### 4.3 配色主题

| 主题 | 说明 |
|------|------|
| **默认** | 浅色主题（白底深字），高对比度，适合老年人 |
| **切换** | 顶部太阳/月亮图标一键切换深色模式（夜间护眼） |

### 4.4 剪辑面板

点击「剪辑」按钮后，弹出剪辑面板：

```
┌─────────────────────────────────┐
│  剪辑 — video_01.mp4            │
├─────────────────────────────────┤
│                                 │
│  保留区间：                     │
│  开始  [02:15:30.500]          │  ← 大号时间输入框
│  结束  [05:45:00.000]          │
│                                 │
│  ──●──────────────○──── 进度条  │  ← 辅助可视化时间轴
│  00:00         05:30           │
│                                 │
│  + 添加删除区间                  │  ← 片段删除
│  删除 [01:00:00.000] ~ [01:30:00.000] ✕ │
│                                 │
│  [▶ 预览]     [✓ 确认]  [✕ 取消] │
└─────────────────────────────────┘
```

---

## 5. 用户工作流

```
启动 App
  │
  ├── 检测草稿 → 是否恢复？
  │     ├── 是 → 恢复上次编辑状态
  │     └── 否 → 空白新任务
  │
  ├── 添加文件（支持多个）
  │     │
  │     ├── 对单个文件：剪辑（可选）
  │     │     ├── 设置保留区间
  │     │     ├── 添加删除区间（可选）
  │     │     └── 预览确认
  │     │
  │     ├── 调整文件排序（拖拽）
  │     │
  │     └── 点击「输出」
  │           ├── 选择输出格式
  │           ├── 选择画质预设
  │           ├── 选择保存路径
  │           ├── 修改文件名（可选）
  │           ├── 查看预估大小
  │           └── 点击「开始导出」
  │                 │
  │                 ├── 显示进度条 + 预估剩余时间
  │                 ├── 支持最小化后台处理
  │                 ├── 支持取消
  │                 └── 完成 → 通知用户
  │
  └── 随时可关闭 App（自动保存草稿）
```

---

## 6. 约束与边界

| 约束 | 说明 |
|------|------|
| **导出上限** | 以输入文件中最高参数为上限，不提供超出源文件规格的选项 |
| **不提升画质** | 不提供超分辨率等功能，避免用户误以为能提升画质 |
| **单文件导出** | 不提供批量导出，每次只导出一个文件 |
| **多文件仅拼接** | 多文件导入的用途只有拼接合并，不做批量编辑 |
| **无网络依赖** | 完全离线运行，不需要联网 |
| **草稿仅一份** | 新任务自动覆盖旧草稿 |

---

## 7. 非功能需求

| 类别 | 要求 |
|------|------|
| **性能** | 大文件处理不阻塞 UI 线程，保持界面响应 |
| **稳定性** | 处理异常时给出明确中文提示，不崩溃 |
| **兼容性** | Windows 10+；Android 8.0+ |
| **安装** | Windows 提供安装包（.exe/.msi）；Android 提供 APK |

---

## 8. 版本规划

| 版本 | 内容 |
|------|------|
| **v1.0** | 文件管理、合并拼接、时间裁剪、片段删除、预览、格式转换、导出、后台处理、自动草稿 |

---

> 📌 **下一步**：PRD 确认无误后进入 UI 设计阶段（高保真原型），然后开始 Flutter 开发。

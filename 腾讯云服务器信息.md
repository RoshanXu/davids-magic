# 腾讯云服务器信息

> 文档更新日期：2026-08-18

## 1. 服务器基本信息

| 项目 | 值 |
|---|---|
| 公网 IP | `1.15.37.168` |
| 主机名 | `VM-12-5-ubuntu` |
| 系统 | Ubuntu 24.04 LTS |
| 内核 | 6.8.0-71-generic |
| 磁盘 | 40G（已用 21G，56%） |
| 内存 | 3.6G（可用约 2.2G）＋ Swap 1.9G |
| Docker | 29.6.2 |
| Docker Compose | v5.3.1 |
| nginx | 1.24.0（宿主直接安装，非容器） |

## 2. 连接方法（SSH）

本机 `~/.ssh/config` 已配置好别名，直接：

```bash
ssh 1.15.37.168
```

对应的 config 内容：

```
Host 1.15.37.168
  HostName 1.15.37.168
  IdentityFile D:/GQY401.pem
  User ubuntu
  Port 22
```

- 登录用户：`ubuntu`
- 私钥：`D:\GQY401.pem`（本机）
- `sudo` 免密（`ubuntu` 用户可直接 `sudo`）

## 3. 服务与端口总览

| 端口（宿主） | 服务 | 说明 |
|---|---|---|
| 22 | sshd | SSH |
| 80 / 443 | nginx（宿主） | 网站入口，443 走 SSL |
| 5000 | transform-app（容器） | 金蝶/精斗云集成后端 HTTP |
| 5443 | transform-app（容器） | 该后端可选 HTTPS（`SSL_CERT_PATH`+`SSL_KEY_PATH` 启用） |
| 3307 → 3306 | transform-mysql（容器） | 金蝶后端的 MySQL |
| 8080 → 80 | davids-magic（容器） | DavidMagic 网页版静态站点 |
| 3306 / 33060 | mysqld（宿主 apt 安装） | ⛔ 已停用并禁止自启（2026-08-18） |

> 注意：宿主上曾有两套 MySQL——`apt` 装的 `mysql-server 8.0.45`（监听 3306）和 Docker 容器 `transform-mysql`（映射 3307）。`transform-app` 实际连接的是**容器内**的 MySQL（服务名 `mysql`，端口 3306）。宿主那套 apt MySQL 已于 2026-08-18 停用并禁止自启（数据保留在 `/var/lib/mysql`，可随时 `systemctl enable --now mysql` 恢复）。

## 4. 域名与 SSL

| 域名 | 状态 |
|---|---|
| `www.pandabox.site` | ✅ 已解析到 1.15.37.168 |
| `pandabox.site`（裸域） | ✅ 已解析到 1.15.37.168（A 记录已加） |
| `open.pandabox.site` | 在 nginx server_name 中（历史遗留） |

- SSL 证书目录：`/etc/nginx/ssl/pandabox/`
  - `pandabox.site_bundle.crt`
  - `pandabox.site.key`
- 有效期：2026-07-16 ～ 2026-10-14（CN = pandabox.site，含 www SAN）

## 5. nginx 站点配置

配置文件在 `/etc/nginx/sites-available/`，启用的是 `flask-proxy`（另有 `default` 兜底）。

**`flask-proxy`（server_name: pandabox.site / www.pandabox.site / open.pandabox.site / 1.15.37.168）**

```nginx
server {
    listen 443 ssl;
    server_name pandabox.site www.pandabox.site open.pandabox.site 1.15.37.168;
    ssl_certificate     /etc/nginx/ssl/pandabox/pandabox.site_bundle.crt;
    ssl_certificate_key /etc/nginx/ssl/pandabox/pandabox.site.key;

    location /api/ {          # → Flask transform-app
        proxy_pass http://127.0.0.1:5000;
        # ... 标准 proxy_set_header
    }

    location = /david {       # 补斜杠
        return 301 /david/;
    }

    location /david/ {        # → DavidMagic 容器
        proxy_pass http://127.0.0.1:8080/;
        # FFmpeg.wasm 依赖 SharedArrayBuffer，必须带这两个头
        add_header Cross-Origin-Opener-Policy same-origin always;
        add_header Cross-Origin-Embedder-Policy require-corp always;
    }

    location / {              # 静态落地页
        root /var/www/landing;
        index index.html;
        try_files $uri $uri/ =404;
    }
}

server {
    listen 80;
    server_name pandabox.site www.pandabox.site open.pandabox.site 1.15.37.168;
    return 301 https://$server_name$request_uri;   # 80 → 443
}
```

**URL 映射一览**

| 路径 | 实际内容 |
|---|---|
| `/` | 落地页「个人的小助手」（`/var/www/landing/index.html`） |
| `/david` | 301 → `/david/` |
| `/david/` | DavidMagic 网页版（`davids-magic` 容器） |
| `/api/*` | transform-app（金蝶/精斗云集成后端） |

## 6. 已部署服务详情

### 6.1 DavidMagic（网页版）

- 容器名：`davids-magic`，镜像 `davids-magic:latest`（135MB）
- 端口：`8080 → 80`（宿主 → 容器）
- 重启策略：`unless-stopped`
- 部署目录：`/home/ubuntu/davids-magic/`（含 `Dockerfile`、`docker-compose.yml`、`docker/`、`web/`）
  - 另有 `/home/ubuntu/DavidsMagic_Docker.tar.gz`（10MB 打包）
- 健康状态：⚠️ 显示 `unhealthy`（健康检查 `wget http://localhost/health` 返回 Connection refused，疑似 localhost 解析到 IPv6 而容器 nginx 只监听 IPv4），**但不影响对外访问**（已实测 8080→80 返回 HTTP 200）

### 6.2 transform-app（金蝶/精斗云集成后端）

- 容器名：`transform-app-1`，镜像 `transform-app`（499MB，`python app.py`）
- 端口：`5000 → 5000`（HTTP）、`5443 → 5443`（可选 HTTPS）
- 重启策略：`unless-stopped`，状态 healthy
- 部署目录：`/home/transform/`，`docker-compose.yml` 关键点：
  - 构建：`smartserver/jdysmart/Dockerfile`
  - 环境变量从 `.env` 读
  - 挂载 `./jdsconfig.py`（金蝶 webhook 回写 appSecret 的文件，**不能设只读**）
- API 路由（均在 `/api/*`）：
  - `POST /api/open/kingdee/subscription`
  - `POST /api/order/sales`
  - `POST /api/order/purchase`
  - `POST /api/receive`
  - `GET  /api/health`
  - `POST /api/sync/customers`
  - `POST /api/sync/materials`
  - `POST /api/sync/suppliers`

### 6.3 MySQL

- 容器 `transform-mysql-1`（镜像 `mysql:8.0`，1.1GB），`3307 → 3306`，数据卷 `mysql_data`，状态 healthy
- 宿主另有 apt 安装的 `mysql-server 8.0.45`（监听 3306/33060），已于 2026-08-18 停用并禁止自启（数据保留）

## 7. 常用运维命令

```bash
# 连接
ssh 1.15.37.168

# 查看容器
docker ps -a

# 改 nginx 后：校验 + 平滑重载
sudo nginx -t
sudo systemctl reload nginx

# DavidMagic 重新构建部署（在 /home/ubuntu/davids-magic 下）
cd /home/ubuntu/davids-magic && docker compose up -d --build

# 快速更新单文件（容器重建即丢失）
docker cp web/index.html davids-magic:/usr/share/nginx/html/index.html

# 查看落地页/nginx 日志
sudo tail -f /var/log/nginx/access.log /var/log/nginx/error.log
```

## 8. 已知问题 / 待办

1. **`davids-magic` 健康检查 unhealthy** → 不影响访问，但 Docker 一直标黄；如需修复，把健康检查改成 `wget -qO- http://127.0.0.1/health` 或让容器 nginx 监听 IPv6。
2. SSL 证书 2026-10-14 到期，需提前续期。

## 9. 配置备份位置

改 `flask-proxy` 前的备份在同目录：
- `/etc/nginx/sites-available/flask-proxy.bak.*`（含 `.bak.landing.*`、`.bak.20260818_*` 等）

services:
  davids-magic:
    build: .
    image: davids-magic:latest
    container_name: davids-magic
    restart: unless-stopped
    ports:
      - "8080:80"
    # 资源限制（可选）
    # deploy:
    #   resources:
    #     limits:
    #       memory: 512M
